openapi: "3.0.3"
info:
  title: Promptsheon API
  description: Version Control System for AI agent intelligence
  # Version 1.0.0 is the production surface. The spec is
  # auto-generated by scripts/genopenapi from the route
  # registrations in promptsheon/handlers/routes.go and the handler
  # bodies in promptsheon/handlers_*.go. Re-run the generator
  # whenever a route or handler changes.
  version: "1.0.0"

security:
  - BearerAuth: []

components:
  securitySchemes:
    BearerAuth:
      type: apiKey
      in: header
      name: Authorization
      description: "API key prefixed with ps_. Send as: Authorization: Bearer ps_..."

  schemas:
    object:
      type: object
      additionalProperties: true
    Error:
      type: object
      properties:
        error:
          type: string

paths:
  /api/v1/alerts/active:
    get:
      operationId: "ListActiveAlerts"
      summary: "ListAlerts lists the alerts."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/alerts/active/{id}/resolve:
    put:
      operationId: "ResolveAlert"
      summary: "ResolveAlert resolves the alert."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/alerts/notifications:
    get:
      operationId: "ListNotificationGroups"
      summary: "ListNotificationGroups lists the notificationGroups."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "AddNotificationGroup"
      summary: "AddNotificationGroup adds the notificationGroup."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                channels:
                  type: array
                  items: {}
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/alerts/rules:
    get:
      operationId: "ListAlertRules"
      summary: "ListAlertRules lists the alertRules."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateAlertRule"
      summary: "CreateAlertRule creates the alertRule."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                type:
                  type: string
                severity:
                  type: string
                threshold:
                  type: number
                duration:
                  type: number
                window:
                  type: number
                config:
                  type: object
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/alerts/rules/{id}:
    get:
      operationId: "GetAlertRule"
      summary: "GetAlertRule returns the alertRule."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateAlertRule"
      summary: "UpdateAlertRule updates the alertRule."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                enabled:
                  type: boolean
                severity:
                  type: string
                threshold:
                  type: number
                config:
                  type: object
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteAlertRule"
      summary: "DeleteAlertRule deletes the alertRule."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/alerts/rules/{rule_id}/groups/{group_id}:
    post:
      operationId: "LinkAlertRuleGroup"
      summary: "handleLinkAlertRuleGroup wires an alert rule to a notification group."
      parameters:
        - name: rule_id
          in: path
          required: true
          schema:
            type: string
        - name: group_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "UnlinkAlertRuleGroup"
      summary: "handleUnlinkAlertRuleGroup removes the wire between an alert rule and a notification group."
      parameters:
        - name: rule_id
          in: path
          required: true
          schema:
            type: string
        - name: group_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/apikeys:
    get:
      operationId: "ListAPIKeys"
      summary: "handleListAPIKeys lists API keys, scoped to the caller's permission level: admins see all keys, non-admins see their own."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateAPIKey"
      summary: "handleCreateAPIKey mints a new API key."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                userID:
                  type: string
                role:
                  type: string
                expiresAt:
                  $ref: "#/components/schemas/object"
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/apikeys/{id}:
    delete:
      operationId: "RevokeAPIKey"
      summary: "RevokeAPIKey revokes an existing API key by ID."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/audit:
    get:
      operationId: "ListAudit"
      summary: "ListAudit lists the audit."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/audit/export:
    get:
      operationId: "ExportAudit"
      summary: "ExportAudit exports the audit."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/audit/verify:
    get:
      operationId: "VerifyAuditChain"
      summary: "VerifyAuditChain verifies the auditChain."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/auth/{provider}/callback:
    get:
      operationId: "OAuthCallback"
      summary: "handleOAuthCallback completes the OAuth login flow."
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/auth/{provider}/login:
    get:
      operationId: "OAuthLoginURL"
      summary: "handleOAuthLogin starts the OAuth login flow by redirecting the caller to the provider's authorization URL with a server-issued state token."
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/capabilities/{capability_id}/datasets:
    get:
      operationId: "ListDatasets"
      summary: "ListDatasets lists the datasets."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateDataset"
      summary: "CreateDataset creates the dataset."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/capabilities/{capability_id}/preconditions:
    get:
      operationId: "ListPreconditions"
      summary: "ListPreconditions lists the preconditions."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreatePrecondition"
      summary: "CreatePrecondition creates the precondition."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/capabilities/{capability_id}/releases:
    get:
      operationId: "ListReleases"
      summary: "ListReleases lists the releases."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/capabilities/{capability_id}/versions:
    get:
      operationId: "ListVersions"
      summary: "handleListVersions returns every immutable Version for the given Capability, ordered by version number (newest first). ListVersions lists the versions."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "AddVersion"
      summary: "CreateVersion creates the version."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                version:
                  type: number
                manifest:
                  $ref: "#/components/schemas/object"
                parents:
                  type: array
                  items: {}
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/capabilities/{capability_id}/versions/latest:
    get:
      operationId: "GetLatestVersion"
      summary: "GetLatestVersion returns the latestVersion."
      parameters:
        - name: capability_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/capabilities/{id}:
    get:
      operationId: "GetCapability"
      summary: "GetCapability returns the capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateCapability"
      summary: "UpdateCapability updates the capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                owner:
                  type: string
                tags:
                  type: array
                  items: {}
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteCapability"
      summary: "DeleteCapability deletes the capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/capabilities/{id}/contract:
    get:
      operationId: "GetCapabilityContract"
      summary: "handleGetCapabilityContract returns the contract attached to a Capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateCapabilityContract"
      summary: "handleUpdateCapabilityContract attaches a contract to a Capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/capabilities/{id}/diff:
    get:
      operationId: "DiffCapabilities"
      summary: "handleDiffVersions returns the structural diff between two Versions of a Capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/capabilities/{id}/reputation:
    get:
      operationId: "GetCapabilityReputation"
      summary: "handleGetCapabilityReputation returns the derived trust score for a Capability."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/capabilities/{id}/self-evolve:
    put:
      operationId: "UpdateSelfEvolveConfig"
      summary: "handleUpdateSelfEvolveConfig is the API backing the `promptsheon selfevolve` CLI subcommand."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/catalog/capabilities:
    get:
      operationId: "CatalogSearch"
      summary: "handleCatalogSearch returns the Capabilities in the supplied workspace whose name matches the query string. Empty query returns every Capability in the workspace.  GET /api/v1/catalog/capabilities?workspace_id=ws1&q=foo&limit=100 CatalogSearch handles the request."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/datasets/{id}:
    get:
      operationId: "GetDataset"
      summary: "GetDataset returns the dataset."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    delete:
      operationId: "DeleteDataset"
      summary: "DeleteDataset deletes the dataset."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/datasets/{id}/cases:
    put:
      operationId: "PutCases"
      summary: "handlePutDatasetCases replaces the cases of a dataset."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/evals/{id}:
    get:
      operationId: "GetEval"
      summary: "GetEval returns the eval."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/executions/{id}:
    get:
      operationId: "GetExecution"
      summary: "GetExecution returns the execution."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/logs/stream:
    get:
      operationId: "LogsStream"
      summary: "handleLogsStream streams backend log entries over Server-Sent Events."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/metrics:
    get:
      operationId: "Metrics"
      summary: "handleMetricsPrometheus returns the metrics collector in the Prometheus text exposition format. MetricsPrometheus handles the request."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/metrics/dashboard:
    get:
      operationId: "MetricsDashboard"
      summary: "handleMetricsSummary returns the in-memory metrics summary (counters, histograms) for the operator dashboard. /metrics/dashboard is an alias for the same payload."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/metrics/summary:
    get:
      operationId: "MetricsSummary"
      summary: "handleMetricsSummary returns the in-memory metrics summary (counters, histograms) for the operator dashboard. /metrics/dashboard is an alias for the same payload."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/preconditions/{id}:
    put:
      operationId: "UpdatePrecondition"
      summary: "handleUpdatePrecondition applies a partial mutation to an existing precondition."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                command:
                  type: string
                timeoutSec:
                  type: number
                enabled:
                  type: boolean
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeletePrecondition"
      summary: "DeletePrecondition deletes the precondition."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/projects/{id}:
    get:
      operationId: "GetProject"
      summary: "GetProject returns the project."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateProject"
      summary: "UpdateProject updates the project."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteProject"
      summary: "DeleteProject deletes the project."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/projects/{project_id}/capabilities:
    get:
      operationId: "ListCapabilities"
      summary: "Capability HTTP handlers (CRUD + self-evolve config). ListCapabilities lists the capabilities."
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateCapability"
      summary: "CreateCapability creates the capability."
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                owner:
                  type: string
                tags:
                  type: array
                  items: {}
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/providers:
    get:
      operationId: "ListProviders"
      summary: "ListProviders lists the providers."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/providers/{name}:
    get:
      operationId: "GetProvider"
      summary: "GetProvider returns the provider."
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/providers/{name}/test:
    post:
      operationId: "TestProvider"
      summary: "handleTestProvider runs a live completion against a registered provider/model pair."
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                model:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/reasoning/compile:
    post:
      operationId: "ReasoningCompile"
      summary: "handleReasoningCompile turns an Intent into a Plan.  POST /api/v1/reasoning/compile Body: Intent JSON 200 OK with Plan 404 Not Found: ErrNoMatch 409 Conflict: ErrConstraintViolation ReasoningCompile handles the request."
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/releases/{id}:
    get:
      operationId: "GetRelease"
      summary: "GetRelease returns the release."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/releases/{id}/activate:
    post:
      operationId: "Activate"
      summary: "ActivateRelease activates the release."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/releases/{id}/approval:
    get:
      operationId: "Approval"
      summary: "GetReleaseApproval returns the releaseApproval."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/releases/{id}/invoke:
    post:
      operationId: "Invoke"
      summary: "InvokeRelease invokes the release."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/releases/{id}/rollback:
    post:
      operationId: "Rollback"
      summary: "RollbackRelease rolls back the release."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/releases/{id}/votes:
    post:
      operationId: "Vote"
      summary: "VoteOnRelease records a vote on the release."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/releases/{release_id}/evals:
    get:
      operationId: "ListEvals"
      summary: "ListEvals lists the evals."
      parameters:
        - name: release_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "RunEval"
      summary: "RunEval runs the eval."
      parameters:
        - name: release_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/settings:
    get:
      operationId: "ListSettings"
      summary: "handleListSettings returns every non-tombstoned key. Secret-shaped values are masked to \"***\" (see internal/settings/secret_keys.go). ListSettings lists the settings."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/settings/{key}:
    get:
      operationId: "GetSetting"
      summary: "handleGetSetting returns the effective value for one key (or 404). Unlike list, which contains persisted live rows only, this endpoint applies environment-over-database precedence."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "SetSetting"
      summary: "handleSetSetting upserts one key."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                value:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteSetting"
      summary: "handleDeleteSetting removes one key."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/setup:
    post:
      operationId: "Setup"
      summary: "handleBootstrap is the first-run endpoint that mints an admin user and an admin API key when the deployment is brand-new and running with authentication disabled."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                name:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/users:
    get:
      operationId: "ListUsers"
      summary: "ListUsers lists the users."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateUser"
      summary: "CreateUser creates the user."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                name:
                  type: string
                role:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/users/{id}:
    get:
      operationId: "GetUser"
      summary: "GetUser returns the user."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateUser"
      summary: "UpdateUser updates the user."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                name:
                  type: string
                role:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteUser"
      summary: "DeleteUser deletes the user."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/vault/keys:
    get:
      operationId: "ListVaultKeys"
      summary: "ListVaultKeys lists the vaultKeys."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "SaveVaultKey"
      summary: "SaveVaultKey saves the vaultKey."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                providerName:
                  type: string
                keyName:
                  type: string
                plaintextKey:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/vault/keys/{id}:
    delete:
      operationId: "DeleteVaultKey"
      summary: "DeleteVaultKey deletes the vaultKey."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/version:
    get:
      operationId: "Version"
      summary: "handleVersion returns the structured build info."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/versions/{id}:
    get:
      operationId: "GetVersion"
      summary: "GetVersion returns the version."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/versions/{version_id}/executions:
    get:
      operationId: "ListExecutions"
      summary: "Auto-split from handlers_capabilities.go ListExecutions lists the executions."
      parameters:
        - name: version_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateExecution"
      summary: "errs.ErrProviderMissing is an alias for the executor's typed sentinel."
      parameters:
        - name: version_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                inputs:
                  type: object
                model:
                  type: string
                provider:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/versions/{version_id}/releases:
    post:
      operationId: "CreateRelease"
      summary: "CreateRelease creates the release."
      parameters:
        - name: version_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/webhooks:
    get:
      operationId: "ListWebhooks"
      summary: "ListWebhooks lists the webhooks."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateWebhook"
      summary: "CreateWebhook creates the webhook."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                uRL:
                  type: string
                events:
                  type: array
                  items: {}
                secret:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/webhooks/{id}:
    delete:
      operationId: "DeleteWebhook"
      summary: "DeleteWebhook deletes the webhook."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/workflows/run:
    post:
      operationId: "RunWorkflow"
      summary: "handleRunWorkflow accepts a workflow Definition and runs it through the configured workflow.Engine."
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/workspaces:
    get:
      operationId: "ListWorkspaces"
      summary: "Auto-split from handlers_capabilities.go ListWorkspaces lists the workspaces."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateWorkspace"
      summary: "CreateWorkspace creates the workspace."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                organization:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/workspaces/{id}:
    get:
      operationId: "GetWorkspace"
      summary: "GetWorkspace returns the workspace."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: "UpdateWorkspace"
      summary: "UpdateWorkspace updates the workspace."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                organization:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
    delete:
      operationId: "DeleteWorkspace"
      summary: "DeleteWorkspace deletes the workspace."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /api/v1/workspaces/{id}/observation:
    get:
      operationId: "GetWorkspaceObservation"
      summary: "handleGetWorkspaceObservation returns the per-Workspace rollup: invocation counts, token totals, error rate, and the most recent execution timestamp."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /api/v1/workspaces/{workspace_id}/projects:
    get:
      operationId: "ListProjects"
      summary: "Auto-split from handlers_capabilities.go ListProjects lists the projects."
      parameters:
        - name: workspace_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: "CreateProject"
      summary: "CreateProject creates the project."
      parameters:
        - name: workspace_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
        "500":
          description: Internal server error
  /health:
    get:
      operationId: "Health"
      summary: "handleHealth is the liveness probe."
      security: []
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /livez:
    get:
      summary: "handleHealth is the liveness probe."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /metrics:
    get:
      summary: "GET metrics"
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /ready:
    get:
      summary: "handleReady returns 200 only when every downstream the daemon depends on for correct operation is reachable:    - The SQLite database responds to Ping (operator's data).   - The OTel tracer is reachable (the daemon otherwise can't     ship traces to the operator's collector; failures here     will not stall the request path but they do mean the     observability story is broken).   - The audit worker queue has room (degraded audit means     we may be losing entries; the queue depth is exposed     so K8s readiness can shed traffic before drops begin).  A failed probe returns 503 with a body naming the failing component so operators can triage from the kubelet log without digging through the daemon log. Ready handles the request."
      security: []
      responses:
        "200":
          description: OK
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
  /readyz:
    get:
      summary: "handleReady returns 200 only when every downstream the daemon depends on for correct operation is reachable:    - The SQLite database responds to Ping (operator's data).   - The OTel tracer is reachable (the daemon otherwise can't     ship traces to the operator's collector; failures here     will not stall the request path but they do mean the     observability story is broken).   - The audit worker queue has room (degraded audit means     we may be losing entries; the queue depth is exposed     so K8s readiness can shed traffic before drops begin).  A failed probe returns 503 with a body naming the failing component so operators can triage from the kubelet log without digging through the daemon log. Ready handles the request."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/object"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
        "404":
          description: Not found
