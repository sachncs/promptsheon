# W02: Database Migrations (Verbatim Port)

**Package:** `@promptsheon/shared`
**Tier:** 0 (Foundation)
**Dependencies:** None (pure SQL)
**Strands Features:** None

---

## Input Contract

- None (pure SQL files)

## Output Contract

- `packages/shared/db/migrations/*.sql` — 21 UP migration files + 1 DOWN
- `packages/shared/db/schema.ts` — TypeScript schema introspection types
- Consumed by W04 (DB connection) to run migrations

---

## Files to Create

### `packages/shared/db/migrations/001_core_schema.up.sql`
Verbatim copy of Go migration `001_core_schema.up.sql`.
36 tables: users, api_keys, provider_keys, audit_entries, audit_chain_state, workspaces, projects, capabilities, capability_versions, executions, releases, approvals, schedules, datasets, dataset_cases, preconditions, eval_runs, eval_results, alert_rules, alerts, notification_groups, alert_rule_notification_groups, recommendations, decisions, lineage_edges, feature_flags, webhook_endpoints, vault_state, ws_state, audit_archive, enforcer_state, idempotency_cache, system_config, bandit_arm_counters, capability_contracts, self_evolve_state.

### `packages/shared/db/migrations/002_audit_chain.up.sql`
`audit_entries_no_update` trigger + timestamp backfill.

### `packages/shared/db/migrations/003_indexes.up.sql`
11 covering/performance indexes.

### `packages/shared/db/migrations/004_data_cleanup.up.sql`
Data normalization backfills.

### `packages/shared/db/migrations/005_seed.up.sql`
System user + feature flag seeds.

### `packages/shared/db/migrations/006_security.up.sql`
No-op on fresh installs.

### `packages/shared/db/migrations/007_views_and_triggers.up.sql`
Reserved no-op.

### `packages/shared/db/migrations/008_destructive_cleanup.up.sql`
Drops pre-v0.1.0 legacy tables (gated).

### `packages/shared/db/migrations/009_vault_state.up.sql`
`vault_state` singleton table.

### `packages/shared/db/migrations/010_ws_state.up.sql`
`ws_state` singleton table.

### `packages/shared/db/migrations/011_audit_archive.up.sql`
`audit_archive` cold storage table.

### `packages/shared/db/migrations/012_enforcer_state.up.sql`
`enforcer_state` budget/quota persistence.

### `packages/shared/db/migrations/013_idempotency_cache.up.sql`
`idempotency_cache` dedup table.

### `packages/shared/db/migrations/014_system_config.up.sql`
`system_config` key/value settings.

### `packages/shared/db/migrations/015_seed_settings.up.sql`
Seed OTel/LLM settings.

### `packages/shared/db/migrations/016_bandit_arm_counters.up.sql`
`bandit_arm_counters` CRDT counters.

### `packages/shared/db/migrations/017_system_config_crdt.up.sql`
Adds CRDT columns to `system_config`.

### `packages/shared/db/migrations/018_capability_contract.up.sql`
`capability_contracts` governance table.

### `packages/shared/db/migrations/019_self_evolve.up.sql`
Self-evolve columns on `capabilities` + `self_evolve_state` table.

### `packages/shared/db/migrations/020_audit_chain_state_immutability.up.sql`
`audit_chain_state_no_update` trigger + `updated_by_app` column.

### `packages/shared/db/migrations/021_canary.up.sql`
`canary_percent` column on `releases`.

### `packages/shared/db/migrations/000_down.sql`
Single DOWN migration that drops all tables (for testing).

### `packages/shared/db/schema.ts`
TypeScript types for introspection:
```typescript
export interface Migration {
  version: number;
  name: string;
  up: string;
  down?: string;
}

export const MIGRATIONS: Migration[] = [
  // Auto-generated from migration files
];
```

---

## Acceptance Criteria

- [ ] All 21 UP migration files are byte-identical copies of Go originals
- [ ] Migration runner processes them in order 001→021
- [ ] Running all migrations on a fresh SQLite DB produces the correct schema
- [ ] All 48 indexes are created
- [ ] All 2 triggers are created
- [ ] All 27 foreign keys are present
- [ ] Seed data is inserted (system user, feature flags, settings)
- [ ] DOWN migration cleanly drops everything
- [ ] No migration depends on application code
