---
const metrics = [
  { value: 1, suffix: '', label: 'release artifact', detail: 'Prompts, tools, policies, and limits move together.' },
  { value: 5, suffix: '', label: 'gated lifecycle steps', detail: 'Author, compile, evaluate, approve, release.' },
  { value: 0, suffix: '', label: 'telemetry by default', detail: 'Your infrastructure, your data, your operating boundary.' },
  { value: 1, suffix: '', label: 'clear rollback path', detail: 'Return to a known content hash when needed.' },
];
---

<section class="ps-section-tight border-y border-[var(--line)] bg-[var(--bg-subtle)]">
  <div class="ps-container">
    <div class="grid grid-cols-2 gap-y-10 lg:grid-cols-4">
      {metrics.map((m, idx) => (
        <div
          class={`ps-reveal px-6 text-center sm:px-8 lg:text-left ${idx > 0 ? 'lg:border-l lg:border-[var(--line)]' : ''}`}
          style={`--reveal-delay: ${idx * 80}ms`}
        >
          <div class="font-mono text-[44px] font-semibold leading-none tracking-tight text-[var(--ink-strong)] sm:text-[52px]">
            <span data-counter data-target={m.value} data-suffix={m.suffix}>{m.value.toLocaleString()}{m.suffix}</span>
          </div>
          <div class="mt-3 text-[12.5px] font-medium uppercase tracking-[0.14em] text-[var(--ink-subtle)]">
            {m.label}
          </div>
          <p class="mx-auto mt-2 max-w-[15rem] text-[12px] leading-relaxed text-[var(--ink-muted)] lg:mx-0">{m.detail}</p>
        </div>
      ))}
    </div>
  </div>
</section>
