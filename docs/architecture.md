# Architecture

Promptsheon is a single-binary HTTP daemon with a layered
internal structure. This page describes the package layout,
the migration timeline, and the integration boundaries.

## System diagram

```
┌──────────────────────────────────────────────────┐
│                   REST API                        │
│         (autogenerated OpenAPI spec)              │
├──────────────────────────────────────────────────┤
│  Auth      │  Rate Limit  │  Audit Log  │  CORS   │
│  Middleware │  Middleware  │  Middleware │         │
├─────────────┴──────────────┴─────────────┴───────┤
│  Capability Mgr  │  Harness   │  Recommendation │  │
│  Manifests       │  Datasets, │  Engine       │  │
│  Releases        │  Precond,  │  (rules +     │  │
│  Approvals       │  Eval Runs │  bandit)      │  │
├──────────────────────────────────────────────────┤
│  Content-Addressable Store  │  SQLite (only)    │
│  (Merkle DAG, pkg/cas/)     │                   │
├──────────────────────────────────────────────────┤
│  LLM Providers  │  Observability  │  Webhooks     │
│  OpenAI/Anthro  │  OTel+Tracing   │  Event-Driven │
│  (official SDKs)│  Prometheus     │  HMAC-signed  │
├──────────────────────────────────────────────────┤
│  Plugin Supervisor  │  Vault  │  KeyProvider     │
│  (gRPC over UDS)   │  (KMS)  │  (BYOK)          │
└──────────────────────────────────────────────────┘
```

## Package layout

| Package | Path | Purpose |
|---------|------|---------|
| `api` | `internal/api/` | HTTP handlers, middleware, server wiring, request/response shapes. |
| `capability` | `internal/capability/` | Workspace, Project, Capability, Version, Release, Manifest value types + repository interface. |
| `release` | `internal/release/` | Release aggregate + application service. |
| `approval` | `internal/approval/` | MakerChecker + Majority policies with fail-closed invariants. |
| `invoke` | `internal/invoke/` | Canonical entry point for one invocation. Enforces Budget + Quota. |
| `executor` | `internal/executor/` | Schedule + webhook → Execution record. The runtime link. |
| `harness` | `internal/harness/` | Dataset, Precondition, EvalRun types + runner. |
| `eval` | `internal/eval/` | Scorer registry (exact_match, contains, regex, json_schema). |
| `cas` | `pkg/cas/` | Content-addressable store (Merkle DAG). Stable public package. |
| `vault` | `internal/vault/` | AES-256-GCM + KMS-backed `KeyProvider` interface. |
| `llm` | `internal/llm/` | OpenAI + Anthropic provider implementations. |
| `webhook` | `internal/webhook/` | Event delivery with HMAC signing + SSRF protection. |
| `observability` | `internal/observability/` | OTel tracing, Prometheus metrics, retention sweep. |
| `metrics` | `internal/metrics/` | The Prometheus collector and HTTP middlewares. |
| `trace` | `internal/trace/` | OTLP-only trace export. |
| `ws` | `internal/ws/` | SSE log stream hub. |
| `ratelimit` | `internal/ratelimit/` | Per-client rate limiting. |
| `auth` | `internal/auth/` | API key authentication + permission model. |
| `pluginsup` | `internal/pluginsup/` | Plugin supervisor (in-process + subprocess). |
| `pluginproto` | `internal/pluginproto/` | gRPC over UDS plugin transport (proto + generated stubs). |
| `subprocess` | `internal/subprocess/` | net/rpc-over-UDS subprocess plugin transport. |
| `recommendation` | `internal/recommendation/` | The deterministic rules engine + bandit Thompson Sampling. |
| `redactor` | `internal/redactor/` | PII redaction default Guardrail. |
| `injection` | `internal/injection/` | Prompt-injection detection default Guardrail. |
| `slo` | `internal/slo/` | Capability-level SLOs. |
| `slo/evaluator` | `internal/slo/evaluator.go` | SLO burn-rate evaluator. |
| `rollups` | `internal/rollups/` | Per-Workspace Budget/Quota rollup aggregator. |
| `rollups/clickhouse` | `internal/rollups/clickhouse/` | ClickHouse writer (build tag `clickhouse`). |
| `bandit` | `internal/bandit/` | Thompson Sampling arm selector. |
| `banditstore` | `internal/banditstore/` | Persistent arm-posterior store. |
| `mcplist` | `internal/mcplist/` | Per-Workspace MCP allowlist. |
| `adoption` | `internal/adoption/` | Per-Workspace Recommendation adoption history. |
| `quota` | `internal/quota/` | Rate-cap enforcement. |
| `budget` | `internal/budget/` | USD-cap enforcement. |
| `lineage` | `internal/lineage/` | Decision + lineage persistence. |
| `eventbus` | `internal/eventbus/` | In-process pub/sub (sync + async). |
| `context` | `internal/context/` | Context assembly manager. |
| `bridge` | `internal/bridge/` | Cross-package adapters. |
| `policy` | `internal/policy/` | Policy decision framework. |
| `replay` | `internal/replay/` | Replay buffer for hash-stable round-trip reproducibility. |
| `schedule` | `internal/schedule/` | Schedule aggregate (cron / webhook / manual). |
| `scheduler` | `internal/scheduler/` | The tick loop. |
| `redactor` | `internal/redactor/` | (see above) |
| `pluginmanifest` | `internal/pluginmanifest/` | PROMPTSHEON_PLUGINS_FILE manifest parser. |
| `plugins/builtins` | `internal/plugins/builtins/` | Default in-process plugins. |
| `observation` | `internal/observation/` | Execution-windowed Observation aggregator. |
| `experiment` | `internal/experiment/` | A/B test scaffolding around `bandit`. |
| `testutil` | `internal/testutil/` | Shared test helpers (logger, sqlite, harness fixture). |
| `testutil/harnessrepo` | `internal/testutil/harnessrepo/` | Shared in-memory `harness.Repository` fixture. |
| `adoption` | `internal/adoption/` | (see above) |
| `webhook` | `internal/webhook/` | (see above) |
| `store` | `internal/store/` | The SQLite-backed `Repository` implementation + migrations. |
| `cli` | `cmd/promptsheon/` | Hand-rolled CLI dispatcher. |
| `daemon` | `cmd/promptsheond/` | The server binary entry point. |
| `healthcheck` | `cmd/promptsheon-healthcheck/` | Container health probe binary. |

## Migrations

The store schema is versioned by `internal/store/migrations/*.up.sql`
files. Migrations 001 through 019 ship today (19 numbered up.sql
files plus their down counterparts where applicable):

| # | Name | Purpose |
|---|------|---------|
| 001 | `core_schema` | Initial tables (workspaces, projects, capabilities, versions, releases, …). |
| 002 | `audit_chain` | Hash-chained audit log. |
| 003 | `indexes` | Lookup indexes. |
| 004 | `data_cleanup` | One-shot cleanup. |
| 005 | `seed` | Seed data for dev. |
| 006 | `security` | API keys + audit_chain_state. |
| 007 | `views_and_triggers` | View layer. |
| 008 | `destructive_cleanup` | Drop v0.0.7 prompts/agents tables (gated by env var). |
| 009 | `vault_state` | Vault state. |
| 010 | `ws_state` | Workspace state. |
| 011 | `audit_archive` | Audit archive (OBS-RET-1). |
| 012 | `enforcer_state` | Persisted Budget/Quota enforcer state (OBS-13). |
| 013 | `idempotency_cache` | Idempotency-Key replay cache (API-IDEMP-1). |
| 014 | `system_config` | System config CRDT. |
| 015 | `seed_settings` | Initial settings seed. |
| 016 | `bandit_arm_counters` | Persistent bandit arm counters. |
| 017 | `system_config_crdt` | Vector-aware merge for system_config. |
| 018 | `capability_contract` | Capability contract schema. |
| 019 | `self_evolve` | Closed-loop self-evolution state. |

To add a new migration, drop a `020_your_migration.up.sql` (and a
`.down.sql`) in `internal/store/migrations/`. The next
`./promptsheond` boot applies it.

## Storage backends

The daemon ships with SQLite (`modernc.org/sqlite`, CGo-free)
as the production store. The Postgres backend lives at
`internal/store/postgres/` and ships the init + RLS SQL
bundles via `LoadSQL()` plus an `InMemoryPostgres` fixture for
contract tests; the live pgx wiring is a follow-on. Operators
that need a live Postgres backend today can satisfy the
`store.Repository` interface in `internal/store/repo.go` — the
repository abstraction lives with the consumer-defined
package pattern (ADR-0012), so a Postgres implementation can
be added without touching the domain packages.

## Provider registry

`internal/llm/Registry` is the consumer-defined provider
registry. The daemon ships with **OpenAI** and **Anthropic**.
To add a new provider, register a factory on the `Registry`
at process startup:

```go
r := llm.NewRegistry()
r.Register("myprovider", func(cfg llm.ProviderConfig) llm.Provider {
    return MyProvider(cfg)
})
```

The `Provider` interface (`Complete(ctx, *Request) (*Response,
error)`) is the integration boundary; an SDK adapter
implementing that interface is all that's required.

## Plugin supervisor

`internal/pluginsup` ties the plugin manifest to the
`internal/supervisor.Supervisor` lifecycle. A manifest entry
with a `binary:` line produces a `subprocess.Binary`; a
manifest entry without a binary line is a pure registration
entry that fails closed (`Remote.Start/Health` return
`errRemoteNotConfigured`) so operators see the gap in
`/metrics` rather than a silent healthy stub.

The gRPC over UDS transport (`internal/pluginproto`) is wired
through `pluginsup.GRPCPlugin`, which dials a plugin binary's
gRPC server and exposes the standard `supervisor.Plugin`
interface. The net/rpc transport remains as a fallback; the
gRPC adapter is the production path for plugin binaries that
want streaming or richer auth.

## Audit chain

The audit log is a **hash chain**, not an append-only list.
Each row records `previous_hash` (the rowid's predecessor's
`entry_hash`) and `entry_hash` (sha256 of the row's canonical
JSON). `store.VerifyAuditChain` walks the chain from rowid 1
forward and asserts the chain invariant.

`audit_archive` (migration 011) is the retention target:
rows older than `PROMPTSHEON_AUDIT_TTL_DAYS` are copied to
`audit_archive` and the source row is preserved. Removing rows
from the middle of the chain is unrecoverable, so the daemon
never deletes. See [docs/security.md](security.md#audit-chain).