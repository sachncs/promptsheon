# Architecture

Promptsheon is a single-binary HTTP daemon with a layered
internal structure. The Go module is one `package main` at the
repo root plus the implementation under `backend/`. This page
describes the package layout, the migration timeline, and the
integration boundaries.

## Source layout

```
promptsheon/
├── main.go                   # Single entry point; dispatches by os.Args[0]
├── daemon.go, cli.go, ...    # All in package main; bin name picks mode
├── backend/                  # Server-side implementation
│   ├── server.go, routes.go  # HTTP server composition + mux registration
│   ├── handlers_*.go         # One HTTP handler file per domain
│   ├── handlers_helpers.go   # validateNonEmpty, parsePagination, ...
│   ├── <pkg>/                # Domain packages (see below)
│   └── store/                # SQLite-backed Repository implementation
└── bin/                      # Build artefacts (promptsheond, promptsheon, promptsheon-healthcheck)
```

`main.go` switches on `os.Args[0]`:

- `promptsheond` → `runDaemon()`
- `promptsheon` → `runCLI()`
- `promptsheon-healthcheck` → `runHealthcheck()`

The three binaries are produced from the same source via
`make build` (which calls `go build -o bin/<name> .` three
times).

## System diagram

```
┌──────────────────────────────────────────────────┐
│                   REST API                        │
│         (autogenerated OpenAPI spec               │
│          backend/spec/spec.yaml)                  │
├──────────────────────────────────────────────────┤
│  Auth      │  Rate Limit  │  Audit Log  │  CORS   │
│  Middleware │  Middleware  │  Middleware │         │
├─────────────┴──────────────┴─────────────┴───────┤
│  Capability Mgr  │  Harness   │  Recommendation │  │
│  Manifests       │  Datasets, │  Engine       │  │
│  Releases        │  Precond,  │  (rules +     │  │
│  Approvals       │  Eval Runs │  bandit)      │  │
├──────────────────────────────────────────────────┤
│  Content-Addressable Store  │  SQLite (only)    │
│  (Merkle DAG, backend/cas/)  │                   │
├──────────────────────────────────────────────────┤
│  LLM Providers  │  Metrics    │  Webhooks       │
│  OpenAI/Anthro  │  Prometheus │  Event-Driven   │
│  (official SDKs)│  OTel       │  HMAC-signed    │
├──────────────────────────────────────────────────┤
│  Plugin Supervisor  │  Vault   │  Schedule       │
│  (backend/plugins) │  (KMS)   │  (cron/webhook) │
└──────────────────────────────────────────────────┘
```

## Package layout

The implementation lives in `backend/`. Sub-packages hold
domain logic; top-level `*.go` files in `backend/` hold the
server composition and HTTP handlers. Files listed without a
trailing slash are single files at `backend/` root, not
sub-directories.

| Package / File | Path | Purpose |
|---|---|---|
| `server` | `backend/server.go` | HTTP server composition, options, mux setup. |
| `routes` | `backend/routes.go` | Single `RegisterRoutes` entry point; every handler is wired here. |
| `handlers_*` | `backend/handlers_*.go` | One HTTP handler file per domain (workspaces, capabilities, releases, harness, ...). |
| `helpers` | `backend/handlers_helpers.go` | `validateNonEmpty`, `parsePagination`, `writeJSON`, `HTTPError`. |
| `capability` | `backend/capability/` | Workspace, Project, Capability, Version, Release, Manifest value types + repository interface. |
| `reference/release.md` | `backend/release/` | Release aggregate + application service. |
| `approval` | `backend/approval/` | MakerChecker + Majority policies with fail-closed invariants. |
| `invoke` | `backend/invoke/` | Canonical entry point for one invocation. Enforces Budget + Quota. |
| `executor` | `backend/executor/` | Schedule + webhook → Execution record. The runtime link. |
| `reference/harness.md` | `backend/harness/` | Dataset, Precondition, EvalRun types + runner. |
| `reference/eval.md` | `backend/eval/` | Scorer registry (exact_match, contains, regex, json_schema). |
| `cas` | `backend/cas/` | Content-addressable store (Merkle DAG). |
| `vault` | `backend/vault/` | AES-256-GCM + KMS-backed `KeyProvider` interface. |
| `llm` | `backend/llm/` | OpenAI + Anthropic provider implementations + `Registry`. |
| `detector` | `backend/detector.go` | Prompt-injection heuristic Guardrail. |
| `redactor` | `backend/redactor.go` | PII redaction Guardrail. |
| `webhook` | `backend/webhook/` | Event delivery with HMAC signing + SSRF protection. |
| `observation` | `backend/observation/` | Windowed `ExecutionRecord` aggregator. |
| `metrics` | `backend/metrics/` | Prometheus collector + HTTP middlewares. |
| `trace` | `backend/trace/` | OTLP trace export. |
| `ratelimit` | `backend/ratelimit/` | Per-client partitioned rate limiter. |
| `auth` | `backend/auth/` | API key authentication + permission model. |
| `plugins` | `backend/plugins/` | Plugin supervisor (in-process + subprocess transport). |
| `supervisor` | `backend/supervisor/` | Plugin lifecycle (restart budget, health gate). |
| `recommendation` | `backend/recommendation/` | Rules engine + bandit Thompson Sampling. |
| `optimizer` | `backend/optimizer/` | Optimizer rules + `CanAutoAdopt`. |
| `rules` | `backend/rules/` | Recommendation rule primitives. |
| `guardrail` | `backend/guardrail/` | Guardrail interface + runner. |
| `quota` | `backend/quota/` | Rate-cap enforcement. |
| `budget` | `backend/budget/` | USD-cap enforcement. |
| `lineage` | `backend/lineage/` | Decision + lineage persistence. |
| `eventbus` | `backend/eventbus/` | In-process pub/sub (sync + async). |
| `replay` | `backend/replay/` | Replay buffer for hash-stable round-trip reproducibility. |
| `schedule` | `backend/schedule/` | Schedule aggregate (cron / webhook / manual). |
| `scheduler` | `backend/scheduler/` | The tick loop. |
| `rollups` | `backend/rollups/` | Per-Workspace Budget/Quota rollup aggregator (in-memory). |
| `bandit` | `backend/bandit/` | Thompson Sampling arm selector. |
| `banditstore` | `backend/banditstore.go` | Persistent arm-posterior store. |
| `mcplist` | `backend/mcplist.go` | Per-Workspace MCP allowlist. |
| `adoption` | `backend/adoption.go` | Per-Workspace Recommendation adoption history. |
| `experiment` | `backend/experiment.go` | A/B test scaffolding around `bandit`. |
| `llmcontext` | `backend/llmcontext.go` | LLM context assembly. |
| `reasoning` | `backend/reasoning.go` | Reasoning compiler primitives. |
| `retention` | `backend/retention.go` | Audit archive retention sweep. |
| `usage` | `backend/usage.go` | `UsageTracker` + Prometheus exposition. |
| `settings` | `backend/settings/` | System config CRDT + resolver. |
| `selfevolve` | `backend/selfevolve/` | Closed-loop self-evolution orchestrator. |
| `search` | `backend/search/` | Catalog search. |
| `alerting` | `backend/alerting/` | Alert rule + notification groups. |
| `policy` | `backend/policy.go` | Policy decision framework. |
| `election` | `backend/election.go` | Leader election for HA. |
| `handler_observation` | `backend/handler_observation.go` | `/api/v1/observations/summary` route + rollup handler. |
| `models` | `backend/models/` | Shared wire types (User, APIKey, ProviderKey, ...). |
| `errs` | `backend/errs/` | Sentinel errors. |
| `testutil` | `backend/testutil/` | Shared test helpers (logger, sqlite, harness fixture). |
| `testutil/harnessrepo` | `backend/testutil/harnessrepo/` | Shared in-memory `harness.Repository` fixture. |
| `store` | `backend/store/` | SQLite-backed `Repository` implementation + migrations. |
| `spec` | `backend/spec/spec.yaml` | Auto-generated OpenAPI 3.0 spec. |

## Migrations

The store schema is versioned by `backend/store/migrations/*.up.sql`
files. The current schema applies migration `019_self_evolve.up.sql`
plus the destructive `008_destructive_cleanup.up.sql` (gated by
`PROMPTSHEON_ALLOW_DESTRUCTIVE_MIGRATIONS=true`).

To add a new migration, drop a `020_your_migration.up.sql` (and a
`.down.sql`) in `backend/store/migrations/`. The next
`./bin/promptsheond` boot applies it.

## Storage backends

The daemon ships with SQLite (`modernc.org/sqlite`, CGo-free)
as the production store. There is no Postgres backend wired in
this build; the `store.Repository` interface in
`backend/store/repo.go` exists so a future pgx-backed
implementation can be added without touching the domain
packages (ADR-0012). Operators that need a different store
today can satisfy that interface in their own fork.

## Provider registry

`backend/llm/Registry` is the consumer-defined provider
registry. The daemon ships with **OpenAI** and **Anthropic**.
To add a new provider, register a factory on the `Registry`
at process startup (see `daemon.go`):

```go
r := llm.NewRegistry()
r.Register("myprovider", func(cfg llm.ProviderConfig) llm.Provider {
    return MyProvider(cfg)
})
```

The `Provider` interface (`Complete(ctx, *Request) (*Response,
error)`) is the integration boundary; an SDK adapter
implementing that interface is all that's required.

## Plugin supervisor

`backend/supervisor.Supervisor` owns the plugin lifecycle:
restart budget, health gate, manifest-driven registration.
The default transport is in-process (`backend/plugins/`); the
`subprocess` package provides net/rpc-over-UDS for plugins
that need isolation. A manifest entry without a working
plugin fails closed so operators see the gap in `/metrics`
rather than a silent healthy stub.

## Audit chain

The audit log is a **hash chain**, not an append-only list.
Each row records `previous_hash` (the rowid's predecessor's
`entry_hash`) and `entry_hash` (sha256 of the row's canonical
JSON). `store.VerifyAuditChain` walks the chain from rowid 1
forward and asserts the chain invariant.

`audit_archive` (migration 011) is the retention target:
rows older than `PROMPTSHEON_AUDIT_TTL_DAYS` are copied to
`audit_archive` and the source row is preserved. Removing rows
from the middle of the chain is unrecoverable, so the daemon
never deletes. See [docs/security.md](../security/security.md#audit-chain).